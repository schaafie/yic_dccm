<h1>Edit Site target</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.site_target_path(@conn, :update, @site_target)) %>

<span><%= link "Back", to: Routes.site_target_path(@conn, :index) %></span>
